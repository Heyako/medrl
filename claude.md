# 角色：资深大模型对齐与强化学习（RLHF）专家
# 任务：共同开发 "MedRL" 医疗长逻辑推理对齐模型项目

你好，Claude！我是一名软件工程师、大模型研究员。我准备启动一个前沿的个人项目，代号为 **"MedRL"**。该项目的核心目标是：面向复杂医疗诊断与病例分析场景，显著提升 **Qwen2.5-7B-Instruct** 基础模型的长链条思维链（CoT）推理能力与价值对齐。

本项目的核心思路深受 OpenAI o1、DeepSeek-R1 以及 HuatuoGPT-o1 的启发。我希望你担任我的资深架构师和联合开发伙伴，帮我从零搭建一整套工业级的训练与评估管线，拒绝任何玩具（Toy）级别的 Demo，我们要直接面向生产环境落地。

以下是该项目的详细技术蓝图。请仔细审阅，我们后续的所有代码编写、架构设计和算法调优都将基于这一框架展开：

---

## 🛠️ 项目核心技术架构

### 1. 数据工程与 PRM（过程监督）数据集构造
* **目标**：将传统的“结果监督（ORM）”全面升级为“过程监督（PRM）”，重构出一套高质量的医疗分步思维链（CoT）数据集。
* **你的任务**：帮我编写鲁棒的 Python 数据清洗脚本。利用 Few-Shot Prompting 驱动闭源大模型 API，将原始的医疗问答轨迹切分为显式的推理步骤（例如用 `\n\nStep X:` 或自定义标签隔离）。同时，引入“大小模型协同验证机制”，自动剔除中间包含逻辑幻觉和推理谬误的步骤，沉淀出步步正确的结构化数据集。

### 2. 强化学习对齐管线（GRPO 与 DAPO 的落地）
* **核心算法**：为了在有限的显存资源下（如单张或少量消费级显卡）训练 7B 模型，我们将彻底放弃传统的 PPO 算法（因为 PPO 需要一个与 Policy 模型同等大小的 Critic 模型，极度消耗显存），全面采用 DeepSeek 验证成功的 **GRPO（组相对策略优化）** 算法。
* **我们要解决的工程痛点**：
    1. **组内奖励同质化**：GRPO 容易导致同一个问题采样出的多个样本得分高度一致，导致方差极小，优势函数（Advantage）失效崩溃。
    2. **奖励骇客（Reward Hacking）**：模型可能会通过无意义的无限循环推理、车轱辘话来“骗取”长度或特定格式的奖励分。
* **复合 Reward 函数设计**：我们需要在 `reward_function.py` 中写出硬核的奖励组合拳：
    - **基于规则的硬约束**：通过严格的正则表达式（Regex）检测推理格式语法（例如是否严格包含 `<thinking>...</thinking><answer>...</answer>` 标签），一旦违规，直接给予严重的负分惩罚。
    - **大模型当裁判（LLM-as-a-Judge）的软约束**：对模型的临床诊断正确性、逻辑连贯性、医学术语规范性进行连续打分。
    - **熵（Entropy）与 KL 散度动态调节**：在代码层面实时监控 KL 散度并引入熵正则项，强效抑制模型退化，确保生成的样本具备足够的多样性（Exploration）。

### 3. 分布式训练与基础设施对接
* **后端框架**：我们将基于 **LLaMA-Factory**、火山引擎的 **verl** 或者是 **OpenRLHF** 进行分布式微调。
* **你的任务**：帮我编写和配置 `deepspeed_config.json`（支持 ZeRO-2/3），利用 FlashAttention-2、Qwen2.5 的 GQA（分组查询注意力）特性以及梯度检查点（Gradient Checkpointing）来极限榨干显存效率，并将我们自定义的复合 Reward 函数完美挂载到框架的训练主循环中。

### 4. 自动化多维评测闭环
* **基准数据集**：在 **MedQA** 和 **MedMCQA** 等医疗核心 Benchmark 上进行定量评估。
* **你的任务**：编写自动化评测脚本，精准提取长推理流 `<thinking>` 标签之后的最终答案（Final Answer），计算精确匹配度（EM Accuracy），并生成统计分析图表。

---

## 🎯 我们的协同工作原则（铁律）

1. **直接输出生产级代码**：当我问你要代码时，拒绝任何伪代码或缺斤少两的示例。请直接输出模块化、带有完整错误处理和日志记录的 PyTorch / HuggingFace / DeepSpeed 代码。
2. **深度技术洞察**：在设计算法时，需要深入底层原理。例如，当组大小（Group Size）变化时，组内优势函数计算公式 $A_i = \frac{R_i - \mu}{\sigma}$ 在数学和数值稳定上会有什么边界影响，请结合代码为我深入剖析。
3. **分阶段、分模块执行**：不要试图一次性把所有代码都塞给我。我们采取“对齐思路 -> 编写模块 -> 联调测试 -> 进入下一阶段”的精细化合作节奏。

---

## 🔥 我们的第一阶段任务：算法机制与项目骨架对齐

为了正式开启 MedRL 项目，请首先为我提供：
1. 一份详尽且通俗易懂的 **GRPO 算法数学原理与架构总结**。重点阐述它在代码层面是如何做到完全不需要 Critic 网络，就能直接在采样组（Group）内部实现相对优势函数计算的。
2. 一个符合生产力标准的**项目标准 Python 目录文件结构设计**，以便我们后续规范化地填充各个功能模块。
3. 创建虚拟环境

如果你已经准备好陪我一起打造 MedRL，请开始你的回答！

注意：本项目灵感来源于复现大佬的简历。简历是这样写的：
Clinical-01:基于Qwen2.5-7B的医疗长逻辑推理与过程监督对齐模型项目负责人自主研发/个人项目2026.02-至今项目简介:面向复杂医疗诊断与病例分析场景,基于Owen2.5-7B-instruct基础模型开展推理能力的强化与价值对齐研究。打通从过程监督(PRM)数据挖掘，到基于前沿强化学习算法(GRPO/DAPO)的完整对齐闭环全面提升模型在长逻辑医疗推理中的准确率与规范性。
承担工作:
数据工程与 PRM 构造:针对医疗复杂推理场景，重构思维链(CoT)数据集范式，由传统的结果监督(ORM)升级为过程监督模型(PRM)。主导设计分步生成的CoT数据链路，并引入“大小模型协同”机制进行细粒度的过程验证(Process Verifcation)，有效剔除中间推理谬误，沉淀了高质量的医疗逻辑数据集。强化学习对齐(RLHF)全链路:独立实施基于GRPO与DAPO的强化学习训练管线。针对GRPO易导致组内奖励同质化、优势函数失效的痛点，创新设计了“格式检査(硬约束)+LLM-as-a-Judge(连续打分)”的复合 Reward 函数,有效拉开 Group 内样本的相对优势差距。同时通过动态调节机制严格控制KL散度并维持Entropy(熵)稳定性，强效抑制了奖励骇客(Reward Hacking)。自动化评测与量化突破::自主搭建完整的自动化多维评测闭环。经过双重对齐优化，模型在医疗核心Benchmark上取得显著突破:在 MedOA 数据集上准确率由 57.0%提升至65.82%，在 MedMCOA 数据集上由 55.6% 提升至 62.0%，大幅增强了复杂病例分析的实际业务可靠性。
项目是根据HuatuoGPT-o1的思路做的sft阶段思维链构造和grouped-base po强化学习。